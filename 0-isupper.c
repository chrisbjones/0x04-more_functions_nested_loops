#include "main.h"
#include <stdio.h>

/*
 *isupper - check for uppercase letters
 *
 *return - (1) if uppercase or (0) if otherwise
 *
 **/

int _isupper(int c)
{
	int a;

	if (a == 'C')
		putchar(1);
	else
		putchar(0);

	return (0);
}
